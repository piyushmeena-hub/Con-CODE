# This file had a git merge conflict and has been superseded.
# The unified backend is in main.py — run that instead:
#
#   python main.py
#
# Keeping this file so existing imports don't break.
from main import app  # re-export for uvicorn compatibility
